import{NextResponse}from'next/server';import{createAdminClient}from'@/lib/supabase/admin';import{verifyRazorpayWebhookSignature}from'@/lib/payments/razorpay';import{jsonSecurityHeaders}from'@/lib/security';const MAX=64000;export async function POST(request:Request){const raw=await request.text();if(new TextEncoder().encode(raw).byteLength>MAX)return NextResponse.json({error:'Webhook body too large'},{status:413,headers:jsonSecurityHeaders()});const signature=request.headers.get('x-razorpay-signature'),eventId=request.headers.get('x-razorpay-event-id'),secret=process.env.RAZORPAY_WEBHOOK_SECRET;if(!signature||!secret)return NextResponse.json({error:'Webhook not configured'},{status:503,headers:jsonSecurityHeaders()});try{if(!verifyRazorpayWebhookSignature(raw,signature,secret))return NextResponse.json({error:'Invalid signature'},{status:400,headers:jsonSecurityHeaders()});const body=JSON.parse(raw),admin=createAdminClient(),providerEventId=eventId||`body_${Buffer.from(raw).toString('base64url').slice(0,80)}`;const{error:insertError}=await admin.from('payment_events').insert({provider_event_id:providerEventId,event_type:String(body?.event||'unknown'),payload:body});if(insertError?.code==='23505')return NextResponse.json({received:true,idempotent:true},{headers:jsonSecurityHeaders()});if(insertError)return NextResponse.json({error:'Unable to persist webhook'},{status:500,headers:jsonSecurityHeaders()});const event=String(body?.event||'');if(event==='payment.captured'||event==='order.paid'){const paymentId=String(body?.payload?.payment?.entity?.id||''),orderId=String(body?.payload?.payment?.entity?.order_id||body?.payload?.order?.entity?.id||'');if(paymentId&&orderId){const{error}=await admin.rpc('grant_payment_credit',{p_order_id:orderId,p_payment_id:paymentId});if(error)return NextResponse.json({error:'Webhook recorded; credit grant pending retry'},{status:500,headers:jsonSecurityHeaders()});}}return NextResponse.json({received:true},{headers:jsonSecurityHeaders()});}catch{return NextResponse.json({error:'Invalid webhook request'},{status:400,headers:jsonSecurityHeaders()});}}
