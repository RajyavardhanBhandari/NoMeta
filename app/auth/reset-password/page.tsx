export default function ResetPasswordPage() { return <main className="auth-page"><div className="auth-card"><div className="eyebrow">Account recovery</div><h1>Choose a new password.</h1><p className="muted">The secure reset flow will validate a single-use recovery token before changing the password.</p><form className="auth-form"><label htmlFor="password">New password</label><input id="password" type="password" minLength={12} required autoComplete="new-password" /><button className="primary-button">Set new password</button></form></div></main>; }
